{
  "name": "todo-today-utilities",
  "version": "1.0.23",
  "main": "build/index.js",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "start": "nodemon --watch src --ext ts --exec \"npx tsc --declaration\"",
    "build": "npx tsc --declaration",
    "deploy": "yarn build && yarn version --patch && yarn publish && cd ../frontend && yarn upgrade todo-today-utilities@latest && cd ../electron && yarn upgrade todo-today-utilities@latest"
  }
}
