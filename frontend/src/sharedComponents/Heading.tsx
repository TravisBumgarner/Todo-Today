import styled from 'styled-components'

import colors from './colors'

const H1 = styled.h1`
    color: ${colors.FOREGROUND_TEXT};
`

const H2 = styled.h2`
    color: ${colors.FOREGROUND_TEXT};
    margin: 1rem 0;
`

const H3 = styled.h3`
    color: ${colors.FOREGROUND_TEXT};
`

export { H1, H2, H3 }
